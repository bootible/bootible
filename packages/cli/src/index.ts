// Phase-1 entry stub. The real verbs (provision / restore / tweak / connect /
// doctor) land in the Phase-1 CLI plan; this exists so the monorepo builds and
// the single-file binary compiles. See docs/v2/plan.md Phase 1.
function main(): void {
  console.log("bootible v2 — dev build. Verbs arrive in a later Phase-1 plan.");
}

main();
