---
description: Archive of bootible v1 documentation — frozen when the v2 redesign began; the shipped v1 system was built and released from these
tags: [bootible, v1, archive, docs]
audience: { human: 60, agent: 40 }
purpose: { reference: 100 }
---

# bootible v1 — Archived Docs

These are the bootible **v1** documents, archived when the v2 redesign began. The v1 system (Steam Deck + ROG Ally, on-device) shipped from these and was still in production at archive time.

| Path | What it is |
|---|---|
| `architecture.md` · `releasing.md` · `checklist-parity.md` · `TROUBLESHOOTING.md` | Reference docs for the live v1 system |
| `superpowers/specs/` · `superpowers/plans/` | v1 design specs + implementation plans (windows-first-hour, live-keys, docs-journey) |
| `research/handheld-community-landscape.md` | v1 market / landscape research |
| `launch/` | v1 launch collateral drafts (reddit post, creator pitches) |
| `docs-persona-review.md` | v1 documentation persona review |

**Notes:**
- Internal links inside these archived docs may point at pre-archive paths (`docs/<file>` rather than `docs/v1/<file>`); they are preserved as historical record, not corrected.
- The active **v2** declaration chain lives at `../v2/`.
- The published user-facing site remains at `../../docs-site/` (unchanged — v1 is still shipping).
- Live code/CI references to `releasing.md` and `checklist-parity.md` were updated to the `docs/v1/` paths during the move.
